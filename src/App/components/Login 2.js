import React from 'react';

const Login = () => (
  <div>
    <h1> HI LOGIN WITH GOOGY</h1>
    <button type="button" onClick={() => alert('you touched the butt')}>
      A BUTTON
    </button>
  </div>
);

export default Login;
